[phases.setup]
nixPkgs = ['python311', 'python311Packages.pip', 'postgresql']

[phases.install]
cmds = ['pip3 install --upgrade pip', 'pip3 install -r requirements.txt']

[phases.build]
cmds = []

[start]
cmd = 'python3 scripts/api-server-v2.py --production --port $PORT'
