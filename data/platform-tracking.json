{
  "_comment": "Where a visitor goes to find out why a platform has no build in the current release, and when it is coming back. Read by scripts/update-version-info.py and merged into version.json -> latest_release.platform_tracking, which public/index.html renders (renderPlatformClaims -> appendAbsenceClause). A pinned value belongs in a data file with a source note, never a script literal.",
  "_rules": [
    "An entry is only ever SHOWN for a platform that has no build in the current release; when the build ships, the platform stops being 'missing' and the link is never rendered. A stale entry is therefore inert, not a lie -- but delete it once its issue closes.",
    "Adding a key here asserts nothing about availability. That is derived from the release assets by derive_platforms(), and this file cannot override it."
  ],
  "platforms": {},
  "_closed_entries_note": "macos was removed 2026-08-28: pdoom1#1309 closed at 21:49Z (an .ico icon Godot could not decode) and v0.14.4 shipped PDoom-macOS-v0.14.4.zip 34 minutes later. This file's own rule is to delete an entry once its issue closes; an entry for a platform that ships is inert rather than wrong, which is exactly the kind of thing that survives for a year because nothing complains."
}
