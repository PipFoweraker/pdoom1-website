{
  "note": "Known divergences that a check SEES, PRINTS, and does not fail on -- each one a decision somebody made on a date, with a date it stops being in force. Read by scripts/acknowledgements.py; see that module's docstring for the rule. The short version: the thing that expires is the ACCEPTANCE, never the finding. Before review_by the check is green and the item is counted in the summary line; after review_by the check is RED on 'this acceptance expired', which is a red a human can always close by deciding something. This file lives at the repo root and NOT under public/, because public/ is rsynced to production and this is CI metadata.",
  "schema": "pdoom-acknowledgements/v1",
  "how_to_add_an_entry": "Every field in scripts/acknowledgements.py REQUIRED_FIELDS is mandatory and non-blank, and `check` must appear in `checks` below -- the loader REFUSES the whole ledger otherwise, rather than skipping the entry, because a skipped entry reads to the next person as a real finding. Pick review_by per entry from when you actually expect to know more; do not give every entry the same date, or they all come due at once and the honest answer becomes a bulk re-accept, which is the same as having no clock.",
  "policy": {
    "warn_within_days": 14,
    "source": "Chosen in PR ci/acknowledgement-clock (2026-08-09) as two working weeks -- long enough that the accepter sees EXPIRING SOON before the cliff and can act deliberately, short enough that the warning is not background noise. Not derived from measurement; revisit if warnings start being ignored."
  },
  "checks": {
    "check-encoding-safety": "scripts/check-encoding-safety.py -- the cp1252 preamble and encoding= sweep. Keys are repo-relative module paths, matching Finding.path.",
    "check-campaign-facts": "scripts/check-campaign-facts.py -- the _facts_this_copy_must_not_break blocks in content/campaigns/. Keys are <campaign file>::<fact id> for entry-scoped findings, and <campaign file>::<finding name> for findings against the copy as a whole. NOTE: the `human` tier's review clock is NOT here -- it lives inline in the campaign file, next to the fact, because the person who acts on it reads that file and nothing else. This ledger holds only findings the check reports and somebody tolerates.",
    "check-mail-auth": "scripts/check-mail-auth.py -- SPF/DKIM/DMARC for the domain that carries feedback mail, plus the M5 interlock that pins the DMARC policy while any PHP mailer would fail alignment. Keys are <domain>/<check id>/<state slug>, e.g. pdoom1.com/M1/absent. The STATE is part of the key on purpose: an acceptance of 'SPF is absent' must not silently forgive 'SPF is present and wrong'. When the state changes the entry goes STALE, which is visible, rather than pre-forgiving a different finding."
  },
  "acknowledgements": [
    {
      "check": "check-campaign-facts",
      "key": "content/campaigns/2026-07-24-alpha-launch.json::copy-defers-a-shipped-platform",
      "what": "The alpha-launch draft copy says 'macOS and Linux within the week' / 'Mac + Linux this week' on all five platforms. Both shipped with v0.14.0 on 2026-08-07, so the copy now defers builds a reader could download today.",
      "why": "The campaign is unposted (approved:false, every posted[*] null) so nothing false has reached a reader, and the fix is a rewrite of Pip's own marketing voice, which is not a seat's to make. The claim understates rather than over-promises -- it sends someone away from a build that exists, which is a smaller harm than promising one that does not, but it is still wrong.",
      "accepted_by": "pdoom1-website seat, PR fix/rotless-campaign-facts. Not Pip's ruling -- he has not been asked yet; this entry is what asking looks like when the asker cannot wait.",
      "accepted_on": "2026-08-14",
      "review_by": "2026-09-15",
      "on_expiry": "Pick one: post the campaign with the platform lines updated, rewrite them, or archive the campaign as superseded by ladder-fork-l4-2026-08. All three close this; none of them is a shrug.",
      "source": "public/data/version.json -> latest_release.platforms (windows/macos/linux all true at v0.14.1); pdoom1/README.md:12-16. Found by scripts/check-campaign-facts.py on the run that introduced it."
    },
    {
      "check": "check-mail-auth",
      "key": "pdoom1.com/M4/absent",
      "what": "google._domainkey.pdoom1.com is NXDOMAIN -- measured 2026-08-15 against ns1.dreamhost.com and re-measured 2026-08-17 after SPF and DMARC went in. CORRECTED 2026-08-17: this entry used to continue 'Nothing the domain sends is DKIM-signed', and that was false. Google signs Workspace mail by default: a real message from team@pdoom1.com to an external Gmail account was observed with `dkim=pass header.i=@pdoom1-com.20251104.gappssmtp.com header.s=20251104`. What is absent is an ALIGNED signature. That d= is not pdoom1.com, so DMARC cannot use it, and the `dmarc=pass` on that message was earned by SPF alignment alone. That distinction is the whole value of this entry: SPF alignment is broken by forwarding, because the forwarder becomes the sender, whereas an aligned DKIM signature survives it. Custom DKIM buys a second, forwarding-proof route to a pass -- it does not fix anything that is broken today.",
      "why": "A DIFFERENT console from the other three: the key is generated in Google Admin (Apps -> Google Workspace -> Gmail -> Authenticate email) and only then pasted into DreamHost DNS, so it is a second act on a second day and it gets its own date rather than joining a herd of expiries that would be answered by one bulk re-accept. Note what DKIM does NOT fix here: mail leaving the DreamHost box never passes through Google, so it can never be signed by this key. DKIM helps the Workspace half only; the intake mailer's sole route to a DMARC pass remains SPF alignment, which is M2 plus M5.",
      "accepted_by": "pdoom1-website seat, branch feedback/intake-contract. Not Pip's ruling.",
      "accepted_on": "2026-08-15",
      "review_by": "2026-08-29",
      "on_expiry": "Ask Pip whether the Google Admin DKIM key was generated and published. If yes, `--live --update` and delete this entry. If the answer is 'we are not using Workspace for this after all', that is a spec change: edit data/mail-auth.json's senders and intended.dkim_selector, and say so in the diff.",
      "source": "docs/decisions/FEEDBACK_INTAKE_CONTRACT.md section 5, 'TXT google._domainkey <generated in Google Admin, not the DreamHost panel>'; measurement in data/mail-auth.json -> observation."
    }
  ]
}
