# Python requirements for p(Doom)1 Website

# ============================================================================
# API Server v1 (Bridge) - No dependencies
# ============================================================================
# The bridge API server (scripts/api-server.py) uses only stdlib:
# - http.server for HTTP handling
# - json for data parsing
# - pathlib for file operations
# - datetime for timestamps

# ============================================================================
# API Server v2 (Production) - PostgreSQL + JWT
# ============================================================================

# Database
psycopg2-binary==2.9.10  # PostgreSQL adapter (updated 2025-11-10)

# Authentication
PyJWT==2.10.1           # JSON Web Tokens (security update 2025-11-10)

# Environment configuration
python-dotenv==1.0.0   # Load .env files

# ============================================================================
# Data contract validation (scripts/validate_data.py, CI)
# ============================================================================
jsonschema==4.26.0     # JSON Schema validation of data seams (schemas/*.schema.json)
Pillow>=10.0.0         # Image optimization (optimize-screenshots.py, blog assets)

# ============================================================================
# Optional Dependencies (Future Features)
# ============================================================================

# redis==5.0.1         # Redis caching (if needed later)
# requests==2.31.0     # HTTP client for external APIs
# gunicorn==21.2.0     # Production WSGI server (alternative to stdlib http.server)
