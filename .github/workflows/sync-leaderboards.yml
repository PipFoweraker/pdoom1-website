name: Sync Leaderboards

# Tier 1 Automation: Fully automatic daily sync
# Syncs all leaderboard data from game repository

on:
  schedule:
    # Daily at 2:00 AM UTC
    - cron: '0 2 * * *'
  workflow_dispatch:
    inputs:
      sync_type:
        description: 'Type of sync to perform'
        type: choice
        default: 'all'
        options:
          - all
          - weekly
          - full

permissions:
  contents: write
  # Without this the "Create issue on failure" step below 403s and the failure is
  # invisible -- CLAUDE.md workflow trap #3. The handler was written correctly and had
  # never been able to run.
  issues: write

jobs:
  sync-leaderboards:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Check game integration status
        id: check-integration
        run: |
          echo "Checking game repository integration..."
          python scripts/game-integration.py --status
          echo "status=checked" >> $GITHUB_OUTPUT

      - name: Sync all leaderboards
        id: sync-all
        if: github.event.inputs.sync_type == 'all' || github.event.inputs.sync_type == '' || github.event_name == 'schedule'
        run: |
          echo "Syncing all leaderboard data..."
          python scripts/game-integration.py --sync-leaderboards
          echo "status=success" >> $GITHUB_OUTPUT
        continue-on-error: true

      - name: Sync weekly league
        id: sync-weekly
        if: github.event.inputs.sync_type == 'weekly' || github.event.inputs.sync_type == 'all' || github.event_name == 'schedule'
        run: |
          echo "Syncing weekly league data..."
          python scripts/game-integration.py --weekly-sync
          echo "status=success" >> $GITHUB_OUTPUT
        continue-on-error: true

      - name: Publish leaderboard snapshot (validated, honest)
        id: export
        if: github.event.inputs.sync_type == 'full'
        run: |
          echo "Publishing leaderboard.json via the real pipeline..."
          pip install --quiet jsonschema==4.26.0 || true
          python scripts/ingest_scores.py
          echo "status=success" >> $GITHUB_OUTPUT
        continue-on-error: true

      - name: Verify data integrity
        id: verify
        run: |
          echo "Verifying leaderboard data integrity..."
          # Check that key files exist
          test -f public/leaderboard/data/leaderboard.json || exit 1
          test -f public/leaderboard/data/weekly/current.json || exit 1
          echo "Data integrity verified"
          echo "status=success" >> $GITHUB_OUTPUT
        continue-on-error: true

      # Every step above is continue-on-error, so the job could never reach a failed
      # state and the `if: failure()` alert at the end was unreachable by construction.
      # continue-on-error is right here -- we want later steps to run and the outcomes
      # logged -- but the outcomes have to be adjudicated somewhere. This is that place.
      - name: Fail the job if a sync step failed
        if: always()
        env:
          SYNC_ALL: ${{ steps.sync-all.outcome }}
          SYNC_WEEKLY: ${{ steps.sync-weekly.outcome }}
          EXPORT: ${{ steps.export.outcome }}
          VERIFY: ${{ steps.verify.outcome }}
        run: |
          bad=0
          for name in SYNC_ALL SYNC_WEEKLY EXPORT VERIFY; do
            outcome="${!name}"
            # "skipped" is normal -- these steps are gated on sync_type.
            if [ "$outcome" = "failure" ]; then
              echo "::error::step $name failed"
              bad=1
            else
              echo "step $name: ${outcome:-not-run}"
            fi
          done
          if [ "$bad" = "1" ]; then
            echo "At least one leaderboard sync step failed. Failing the job so the"
            echo "alert below actually fires instead of reporting a green sync."
            exit 1
          fi

      - name: Log automation run
        if: always()
        run: |
          echo "Logging leaderboard sync to monitoring..."
          python scripts/log-automation-run.py \
            --job "sync-leaderboards" \
            --sync-all-status "${{ steps.sync-all.outcome }}" \
            --sync-weekly-status "${{ steps.sync-weekly.outcome }}" \
            --export-status "${{ steps.export.outcome }}" \
            --verify-status "${{ steps.verify.outcome }}" \
            --trigger "${{ github.event_name }}"
        continue-on-error: true

      - name: Commit synced data
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action [Leaderboard Sync]"

          # CLAUDE.md workflow trap #1: `git diff` cannot see UNTRACKED files, so testing
          # it before `git add` reports "no changes" whenever the sync created a new file
          # -- and reports SUCCESS while committing nothing. Stage first, then test the
          # index. board-liveness.json in particular starts life untracked.
          git add public/leaderboard/data/
          git add public/monitoring/data/ || true

          if git diff --cached --quiet; then
            echo "No changes to commit (data already up to date)"
            exit 0
          fi

          # Count changed files
          CHANGED=$(git diff --cached --numstat | wc -l)

          git commit -m "Sync leaderboards: ${CHANGED} files updated

          Sync all: ${{ steps.sync-all.outcome }}
          Sync weekly: ${{ steps.sync-weekly.outcome }}
          Export: ${{ steps.export.outcome }}
          Verification: ${{ steps.verify.outcome }}
          Timestamp: $(date -u +'%Y-%m-%d %H:%M:%S UTC')

          [automated] [leaderboard-sync]"

          git push

      - name: Create issue on failure
        if: failure()
        uses: actions/github-script@v7
        with:
          script: |
            // De-duplicated alert: one rolling issue (fixed title, no date), comment on repeats.
            const title = '🔄 Leaderboard sync failing';
            const label = 'sync-failure';
            const runUrl = `${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}`;
            const body = [
              '## Leaderboard sync failure',
              '',
              `all: ${{ steps.sync-all.outcome }} | weekly: ${{ steps.sync-weekly.outcome }} | verify: ${{ steps.verify.outcome }}`,
              `Run: ${runUrl}`,
              '',
              'Likely: game repo inaccessible, data-format change, or the game->website uplift (see the uplift tracking issue).',
              '',
              '_Single rolling issue -- reused, not duplicated, per run._'
            ].join('\n');
            const existing = await github.rest.issues.listForRepo({
              owner: context.repo.owner, repo: context.repo.repo,
              state: 'open', labels: label, per_page: 20,
            });
            const found = existing.data.find(i => i.title === title);
            if (found) {
              await github.rest.issues.createComment({
                owner: context.repo.owner, repo: context.repo.repo,
                issue_number: found.number,
                body: `Still failing as of ${new Date().toUTCString()}.\n\nRun: ${runUrl}`,
              });
            } else {
              await github.rest.issues.create({
                owner: context.repo.owner, repo: context.repo.repo,
                title, body, labels: ['automation', 'leaderboard', label],
              });
            }
